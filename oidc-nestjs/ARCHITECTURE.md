# 🏗️ Architecture Diagram

## System Architecture

```
┌─────────────────────────────────────────────────────────────────┐
│                        Client Applications                       │
│  ┌──────────────────┐              ┌──────────────────┐         │
│  │  Admin Dashboard │              │   Test Client    │         │
│  │  (React - PKCE)  │              │  (Confidential)  │         │
│  │  localhost:4000  │              │  localhost:3001  │         │
│  └────────┬─────────┘              └─────────┬────────┘         │
└───────────┼───────────────────────────────────┼─────────────────┘
            │                                   │
            │  Authorization Code + PKCE        │  Authorization Code
            │                                   │
            └───────────────┬───────────────────┘
                            │
                            ▼
    ┌───────────────────────────────────────────────────────┐
    │                                                        │
    │         NestJS OpenID Connect Provider                │
    │              http://localhost:3000                    │
    │                                                        │
    │  ┌─────────────────────────────────────────────────┐  │
    │  │              Main Application                   │  │
    │  │                  main.ts                        │  │
    │  │  ┌──────────────────────────────────────────┐  │  │
    │  │  │        OIDC Provider Instance            │  │  │
    │  │  │         (oidc-provider v8)               │  │  │
    │  │  │                                          │  │  │
    │  │  │  Endpoints:                              │  │  │
    │  │  │  • /auth         (Authorization)         │  │  │
    │  │  │  • /token        (Token Exchange)        │  │  │
    │  │  │  • /me           (UserInfo)              │  │  │
    │  │  │  • /.well-known  (Discovery & JWKS)      │  │  │
    │  │  └──────────────────────────────────────────┘  │  │
    │  └─────────────────────────────────────────────────┘  │
    │                                                        │
    │  ┌─────────────────────────────────────────────────┐  │
    │  │            Controllers Layer                    │  │
    │  │  ┌──────────────────────────────────────────┐  │  │
    │  │  │    InteractionController                 │  │  │
    │  │  │    • GET  /interaction/:uid              │  │  │
    │  │  │    • POST /interaction/:uid/login        │  │  │
    │  │  │    • POST /interaction/:uid/consent      │  │  │
    │  │  └──────────────────────────────────────────┘  │  │
    │  └─────────────────────────────────────────────────┘  │
    │                         │                             │
    │                         ▼                             │
    │  ┌─────────────────────────────────────────────────┐  │
    │  │              Service Layer                      │  │
    │  │  ┌────────────────┐    ┌────────────────┐      │  │
    │  │  │  UserService   │    │  OidcService   │      │  │
    │  │  │  • findById    │    │  • findApp     │      │  │
    │  │  │  • authenticate│    │  • createToken │      │  │
    │  │  │  • validate    │    │  • createAuth  │      │  │
    │  │  └────────────────┘    └────────────────┘      │  │
    │  └─────────────────────────────────────────────────┘  │
    │                         │                             │
    │                         ▼                             │
    │  ┌─────────────────────────────────────────────────┐  │
    │  │              Adapter Layer                      │  │
    │  │  ┌──────────────────────────────────────────┐  │  │
    │  │  │          OidcAdapter                     │  │  │
    │  │  │  Maps oidc-provider to TypeORM           │  │  │
    │  │  │  • upsert()  • find()                    │  │  │
    │  │  │  • destroy() • revokeByGrantId()         │  │  │
    │  │  └──────────────────────────────────────────┘  │  │
    │  └─────────────────────────────────────────────────┘  │
    │                         │                             │
    │                         ▼                             │
    │  ┌─────────────────────────────────────────────────┐  │
    │  │           TypeORM Repositories                  │  │
    │  │  ┌──────────────────────────────────────────┐  │  │
    │  │  │  ApplicationRepository                   │  │  │
    │  │  │  AuthorizationRepository                 │  │  │
    │  │  │  ScopeRepository                         │  │  │
    │  │  │  TokenRepository                         │  │  │
    │  │  │  UserRepository                          │  │  │
    │  │  └──────────────────────────────────────────┘  │  │
    │  └─────────────────────────────────────────────────┘  │
    └────────────────────────┬───────────────────────────────┘
                             │
                             ▼
         ┌───────────────────────────────────────────┐
         │           MySQL Database                  │
         │                                           │
         │  ┌─────────────────────────────────────┐  │
         │  │  openiddict_applications            │  │
         │  │  • id, client_id, client_secret     │  │
         │  │  • permissions, redirect_uris       │  │
         │  └─────────────────────────────────────┘  │
         │                                           │
         │  ┌─────────────────────────────────────┐  │
         │  │  openiddict_authorizations          │  │
         │  │  • id, application_id               │  │
         │  │  • subject, scopes, status          │  │
         │  └─────────────────────────────────────┘  │
         │                                           │
         │  ┌─────────────────────────────────────┐  │
         │  │  openiddict_scopes                  │  │
         │  │  • id, name, display_name           │  │
         │  └─────────────────────────────────────┘  │
         │                                           │
         │  ┌─────────────────────────────────────┐  │
         │  │  openiddict_tokens                  │  │
         │  │  • id, application_id, auth_id      │  │
         │  │  • payload, expiration, status      │  │
         │  └─────────────────────────────────────┘  │
         │                                           │
         │  ┌─────────────────────────────────────┐  │
         │  │  users                              │  │
         │  │  • id, username, password_hash      │  │
         │  │  • email, name, is_active           │  │
         │  └─────────────────────────────────────┘  │
         └───────────────────────────────────────────┘
```

## Authentication Flow

```
┌──────────┐                                        ┌──────────────┐
│  Client  │                                        │ OIDC Provider│
│   App    │                                        │   (NestJS)   │
└────┬─────┘                                        └──────┬───────┘
     │                                                     │
     │  1. GET /auth?client_id=xxx&redirect_uri=xxx       │
     │  ──────────────────────────────────────────────>   │
     │                                                     │
     │  2. 302 Redirect to /interaction/:uid              │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  3. GET /interaction/:uid (Show Login Form)        │
     │  ──────────────────────────────────────────────>   │
     │                                                     │
     │  4. HTML Login Form                                │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  5. POST /interaction/:uid/login                   │
     │     (username, password)                           │
     │  ──────────────────────────────────────────────>   │
     │                         │                          │
     │                         ├──> UserService.authenticate()
     │                         │                          │
     │  6. 302 Redirect back to /auth (authenticated)     │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  7. Show Consent Screen (if needed)                │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  8. POST /interaction/:uid/consent                 │
     │  ──────────────────────────────────────────────>   │
     │                         │                          │
     │                         ├──> OidcService.createAuthorization()
     │                         │                          │
     │  9. 302 Redirect with authorization code           │
     │     Location: redirect_uri?code=xxx&state=yyy      │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  10. POST /token (Exchange code for tokens)        │
     │      code=xxx&code_verifier=zzz (PKCE)             │
     │  ──────────────────────────────────────────────>   │
     │                         │                          │
     │                         ├──> OidcAdapter.upsert()
     │                         ├──> TokenRepository.save()
     │                         │                          │
     │  11. JSON Response                                 │
     │      { access_token, id_token, refresh_token }     │
     │  <──────────────────────────────────────────────   │
     │                                                     │
     │  12. GET /me (Get user info)                       │
     │      Authorization: Bearer <access_token>          │
     │  ──────────────────────────────────────────────>   │
     │                         │                          │
     │                         ├──> UserService.findById()
     │                         │                          │
     │  13. JSON Response                                 │
     │      { sub, email, name, ... }                     │
     │  <──────────────────────────────────────────────   │
     │                                                     │
```

## Database Entity Relationships

```
┌─────────────────────────┐
│  openiddict_applications│
│  ─────────────────────  │
│  • id (PK)              │
│  • client_id (UNIQUE)   │
│  • client_secret        │
│  • permissions          │
│  • redirect_uris        │
└────────┬────────────────┘
         │
         │ 1:N (application_id)
         │
    ┌────┴──────────────────────────────┐
    │                                   │
    ▼                                   ▼
┌────────────────────────┐    ┌─────────────────────┐
│openiddict_authorizations│    │  openiddict_tokens  │
│ ──────────────────────  │    │  ─────────────────  │
│ • id (PK)               │    │  • id (PK)          │
│ • application_id (FK)   │    │  • application_id   │
│ • subject (user_id)     │───>│  • authorization_id │
│ • scopes                │ 1:N│  • payload          │
│ • status                │    │  • expiration_date  │
└─────────────────────────┘    │  • status           │
                               │  • type             │
                               └─────────────────────┘

┌──────────────────┐
│ openiddict_scopes│         (Referenced by name in
│ ────────────────  │          authorization.scopes)
│ • id (PK)        │
│ • name (UNIQUE)  │
│ • display_name   │
└──────────────────┘

┌──────────────────┐
│      users       │         (Referenced by id in
│ ────────────────  │          authorization.subject)
│ • id (PK)        │
│ • username       │
│ • password_hash  │
│ • email          │
└──────────────────┘
```

## Module Dependencies

```
                    AppModule (Root)
                         │
         ┌───────────────┼───────────────┐
         │               │               │
         ▼               ▼               ▼
    ConfigModule    TypeOrmModule   UserModule
    (Global)        (Database)          │
                         │              │
                         │              ▼
                         │        UserService
                         │              │
                         │              │
                         ▼              │
                    OidcModule          │
                         │              │
                    ┌────┴────┐         │
                    ▼         ▼         │
              OidcService  OidcAdapter  │
                    │         │         │
                    └─────────┴─────────┘
                              │
                              ▼
                   InteractionController
```

## Request Flow Example

```
User clicks "Sign in with OpenID Connect" in Admin App
    │
    ├──> Admin generates PKCE code_verifier & code_challenge
    │
    ├──> Redirects to: http://localhost:3000/auth?
    │         client_id=admin_client
    │         &response_type=code
    │         &redirect_uri=http://localhost:4000/callback
    │         &scope=openid profile email
    │         &state=random_state
    │         &code_challenge=challenge_value
    │         &code_challenge_method=S256
    │
    ▼
OIDC Provider receives request
    │
    ├──> Validates client_id (admin_client exists?)
    │
    ├──> Checks if user is authenticated (session?)
    │
    ├──> No session → Creates interaction
    │
    ├──> Redirects to /interaction/:uid
    │
    ▼
InteractionController shows login form
    │
    ├──> User enters: demo / demo123
    │
    ├──> POST /interaction/:uid/login
    │
    ├──> UserService.authenticate(demo, demo123)
    │         │
    │         ├──> UserRepository.findByUsername('demo')
    │         │
    │         ├──> bcrypt.compare(demo123, user.password_hash)
    │         │
    │         └──> Returns user object
    │
    ├──> Session established with user.id
    │
    ├──> Shows consent screen (if needed)
    │
    ├──> User approves → POST /interaction/:uid/consent
    │
    ├──> Creates Grant (Authorization)
    │         │
    │         └──> OidcService.createAuthorization()
    │                   │
    │                   └──> AuthorizationRepository.save()
    │
    ├──> Generates authorization code
    │         │
    │         └──> OidcAdapter.upsert() → TokenRepository
    │
    ├──> Redirects to: http://localhost:4000/callback?
    │         code=auth_code_value
    │         &state=random_state
    │
    ▼
Admin App receives code
    │
    ├──> POST http://localhost:3000/token
    │         grant_type=authorization_code
    │         &code=auth_code_value
    │         &redirect_uri=http://localhost:4000/callback
    │         &client_id=admin_client
    │         &code_verifier=verifier_value
    │
    ▼
OIDC Provider validates
    │
    ├──> Validates code_verifier matches code_challenge
    │
    ├──> Retrieves authorization_code from TokenRepository
    │
    ├──> Generates access_token, id_token, refresh_token
    │
    ├──> Stores tokens in TokenRepository
    │
    ├──> Returns JSON:
    │         {
    │           access_token: "...",
    │           id_token: "...",
    │           refresh_token: "...",
    │           token_type: "Bearer",
    │           expires_in: 3600
    │         }
    │
    ▼
Admin App stores tokens & redirects to dashboard
    │
    ├──> Can now call: GET http://localhost:3000/me
    │         Authorization: Bearer <access_token>
    │
    └──> Receives user info:
              {
                sub: "user_id",
                email: "demo@example.com",
                name: "Demo User"
              }
```
